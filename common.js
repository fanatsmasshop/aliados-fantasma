import {supabase} from "./supabase-client.js";export const qs=(s,r=document)=>r.querySelector(s);export const qsa=(s,r=document)=>[...r.querySelectorAll(s)];export function escapeHtml(v=""){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}export function showMessage(e,m,t=""){if(!e)return;e.textContent=m;e.className=`notice ${t}`.trim();e.classList.remove("hidden")}export function normalizeUrl(v){if(!v)return null;try{const u=new URL(v);return["http:","https:"].includes(u.protocol)?u.toString():null}catch{return null}}export async function getSession(){const{data,error}=await supabase.auth.getSession();if(error)throw error;return data.session}export async function requireAuth(){const s=await getSession();if(!s){location.href="login.html";throw new Error("Sin sesión")}return s}export async function getProfile(){const s=await requireAuth();const{data,error}=await supabase.from("perfiles").select("*").eq("id",s.user.id).single();if(error)throw error;return data}export async function requireAdmin(){const p=await getProfile();if(p.rol!=="administrador"||!p.activo){document.body.innerHTML='<main class="auth-shell"><section class="card auth-card"><h1>Acceso restringido</h1><p class="muted">Tu cuenta no tiene permisos de administrador.</p></section></main>';throw new Error("No administrador")}return p}export async function logout(){await supabase.auth.signOut();location.href="login.html"}export{supabase};
