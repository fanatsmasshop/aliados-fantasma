import { supabase } from './supabase-client.js?v=20260717-2';import { isConfigured } from './config.js?v=20260717-2';const form=document.querySelector('#login-form'),email=document.querySelector('#email'),password=document.querySelector('#password'),button=document.querySelector('#login-button'),message=document.querySelector('#login-message'),warning=document.querySelector('#config-warning');if(!isConfigured||!supabase){warning.classList.remove('hidden');button.disabled=true}else{const{data}=await supabase.auth.getSession();if(data.session)await redirectByRole(data.session.user)}document.querySelector('#toggle-password').addEventListener('click',e=>{const show=password.type==='password';password.type=show?'text':'password';e.currentTarget.textContent=show?'Ocultar':'Ver'});async function redirectByRole(user){const{data:profile}=await supabase.from('perfiles').select('rol,activo,estado').eq('id',user.id).maybeSingle();if(profile?.rol==='administrador'&&profile?.activo===true){location.replace('dashboard.html');return}if(profile?.rol==='propietario'&&profile?.activo===true&&profile?.estado==='activo'){location.replace('panel.html');return}location.replace('estado-cuenta.html')}form.addEventListener('submit',async e=>{e.preventDefault();message.textContent='';button.disabled=true;button.querySelector('span').textContent='Ingresando…';try{const{data,error}=await supabase.auth.signInWithPassword({email:email.value.trim().toLowerCase(),password:password.value});if(error){const raw=(error.message||'').toLowerCase();message.textContent=raw.includes('email not confirmed')?'Debes confirmar tu correo antes de entrar.':raw.includes('invalid login')?'El correo o la contraseña son incorrectos.':'No pudimos iniciar sesión. Inténtalo nuevamente.';return}await redirectByRole(data.user)}catch(e){console.error(e);message.textContent='No fue posible conectar con el servicio de acceso.'}finally{button.disabled=false;button.querySelector('span').textContent='Entrar'}});